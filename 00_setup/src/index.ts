function welcomeExchange(userId:string,exchange:string): string{
    return `Hey ${userId}, Welocome to ${exchange}`;
}

const userid:string ="ROHIT";
const exchangeName:string="MSEI";

console.log(welcomeExchange(userid,exchangeName));

